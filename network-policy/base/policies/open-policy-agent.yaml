apiVersion: crd.projectcalico.org/v1
kind: NetworkPolicy
metadata:
  name: ingress-open-policy-agent
  namespace: open-policy-agent
spec:
  order: 1000.0
  selector: app == 'opa'
  types:
    - Ingress
  ingress:
    - action: Allow
      protocol: TCP
      destination:
        ports:
          - 8443