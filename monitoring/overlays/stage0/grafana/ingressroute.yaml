apiVersion: contour.heptio.com/v1beta1
kind: IngressRoute
metadata:
  name: grafana
  namespace: monitoring
  annotations:
    kubernetes.io/tls-acme: "true"
spec:
  virtualhost:
    fqdn: grafana.monitoring.stage0.cybozu-ne.co
    tls:
      secretName: grafana-tls
