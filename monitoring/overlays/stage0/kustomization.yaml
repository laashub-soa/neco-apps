apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
bases:
  - ../../base
patches:
  - grafana/pvc.yaml
  - prometheus/pvc.yaml
resources:
  - grafana/ingressroute.yaml
configMapGenerator:
  - name: alertmanager
    behavior: merge
    files:
      - alertmanager/env.template
