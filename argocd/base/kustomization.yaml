apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - upstream/base
  - upstream/crds
  - upstream/cluster-rbac
patchesStrategicMerge:
  - configmap.yaml
  - deployment.yaml
  - serviceaccount.yaml
  - rbac.yaml
  - secret.yaml
  - service.yaml
