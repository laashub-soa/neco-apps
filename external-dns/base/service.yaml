apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: metrics
    app.kubernetes.io/name: external-dns-metrics
    app.kubernetes.io/part-of: external-dns
  name: external-dns-metrics
spec:
  ports:
    - name: metrics
      port: 7979
      protocol: TCP
      targetPort: 7979
  selector:
    app.kubernetes.io/name: external-dns
