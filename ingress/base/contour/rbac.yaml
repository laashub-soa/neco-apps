apiVersion: rbac.authorization.k8s.io/v1beta1
kind: ClusterRoleBinding
metadata:
  name: contour
subjects:
  - $patch: replace
    kind: ServiceAccount
    name: contour
    namespace: ingress
---
apiVersion: rbac.authorization.k8s.io/v1beta1
kind: ClusterRoleBinding
metadata:
  name: contour
subjects:
  - $patch: replace
    kind: ServiceAccount
    name: contour
    namespace: ingress
